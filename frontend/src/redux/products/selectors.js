export const selectProducts = (state) => state.product.products;

export const selectProductDetails = (state) => state.product.productDetails;

export const selectTotalCount = (state) => state.product.totalCount;

export const selectLoading = (state) => state.product.loading;

export const selectError = (state) => state.product.error;

export const selectCreateSuccess = (state) => state.product.createSuccess;

export const selectCreatedProduct = (state) => state.product.createdProduct;
